# Kotlin Matrix Calculator 🧮

A simple matrix calculator built with Kotlin. Supports:

- Matrix addition, subtraction, multiplication
- Transpose and inverse
- LU decomposition
- CLI-based usage or simple UI (Compose/Desktop)

## Run

```bash
./gradlew run
